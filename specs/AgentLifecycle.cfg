\* Correct configuration: both faults disabled. TLC must find NO invariant
\* violation. This is the green oracle for the broker lifecycle state machine.
SPECIFICATION Spec

CONSTANTS
    Agents = {a1, a2}
    Consumers = {c1, c2}
    AllowReapWhileHeld = FALSE
    AllowStealGrant = FALSE

INVARIANTS
    TypeOK
    Inv_NoDoubleOwnership
    Inv_NoHeldReap
