\* Mutation 2: steal-grant enabled. TLC MUST report an Inv_NoDoubleOwnership
\* violation (one agent held by two consumers). If this run passes, the
\* invariant is toothless. verify-specs asserts this run fails.
SPECIFICATION Spec

CONSTANTS
    Agents = {a1, a2}
    Consumers = {c1, c2}
    AllowReapWhileHeld = FALSE
    AllowStealGrant = TRUE

INVARIANTS
    TypeOK
    Inv_NoDoubleOwnership
    Inv_NoHeldReap
