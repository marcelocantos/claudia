# v0.28.0

### Added
- `ListCursorModels` — offline-first Cursor model catalog (disk cache + embedded snapshot; CLI refresh via `Refresh` / `MaxAge`).
- Cursor ACP leftover-writer reap after Session stop.
- Ledger permission-tool parsing helpers for ACP permission gating.

### Fixed
- Claude Task print-mode with `DisallowTools`: terminate argv with `--` so Claude Code 2.1.x variadic `--disallowedTools` cannot consume the prompt (ytt empty-result / 🎯T49). Failed print-mode stderr is now a `TaskEventError`, not Debug-only.
- Codex Session: persist start-time rollout identity so resume/reap can find the live turn.
- Cursor ACP process cleanup: reap leftover writer processes after stop.

### Changed
- Session MCP exclusive homes and registry attach stay private to `Config.MCPServers` (continuing v0.27.0 policy); Codex/Grok/Cursor paths hardened around exclusive attach and leftover processes.

### Docs
- `AGENTS.md` live gate table includes `TestClaudeTaskDisallowToolsLiveSmoke`.
