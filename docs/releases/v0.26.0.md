### Added
- `ProviderCursor`: Session via `agent acp` and Task via `agent --print --output-format stream-json`.
- Cursor MCP: `LoadMCP` / `EnsureMCP` for `~/.cursor/mcp.json`; exclusive mode uses project `.cursor/mcp.json` (no HOME rewrite).
- Opt-in Cursor plan usage (`CLAUDIA_CURSOR_USAGE=1` / `CursorUnstableUsage`).
- `Config.MCPExclusive` / `AgentDef.MCPExclusive` isolates Session MCP (Grok/Codex homes; Cursor project mcp).
- `RefreshMCPToken` plus `MCPProxy.SetToken` / `OnTokenChange` for host-persisted OAuth refresh.
- `CloseGoal` / `GoalCompleteCheck` / `ParseGoalStatus` so hosts can stop Continue when done.
- Hermetic Cursor ACP/print fakes and live gates (`CLAUDIA_CURSOR_LIVE=1`).

### Fixed
- Live MCP mnemo gate: affirmative MNEMO-OK / mcp__mnemo only; prefer direct mnemo daemon URL when jevons upstream is down.
- `MCPProxy` concurrent 401 probe race (ENT-001).
- Codex binary discovery skips cmux CLI shims; prefers known install paths.
- Goal continuation kept under Claude's paste-chip size threshold.

### Docs
- `docs/cursor-acp-session.md`, `docs/cursor-provider-oracle-map.md`, agents-guide / README / AGENTS live-gate tables.
