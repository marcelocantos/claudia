#!/bin/sh
# Pre-push: run make gate (hermetic suite). The hook executes the suite;
# it does not read a "tests passed" claim. Agents must not pass
# --no-verify. Owner emergency bypass is git push --no-verify.
set -e
cd "$(git rev-parse --show-toplevel)"
make gate
